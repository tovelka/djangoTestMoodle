events {}

http {
    include /etc/nginx/mime.types;
    server_tokens off;
    client_max_body_size 25M;

    upstream django {
        server web:8000;
    }

    server {
        listen 80;
        server_name tovelka.ru www.tovelka.ru;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name tovelka.ru www.tovelka.ru;

        ssl_certificate /etc/letsencrypt/live/tovelka.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/tovelka.ru/privkey.pem;

        location / {
            proxy_pass http://django;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /static/ {
            alias /app/static/;
        }

    }
}
